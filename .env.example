# HanVoice — copy to .env and fill in. Used by docker-compose; the backend
# also reads backend/.env directly when run outside Docker.

# --- App ---
APP_ENV=development
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:5173,http://localhost:8080
FRONTEND_URL=http://localhost:5173

# --- Supabase (Settings > API in the dashboard) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret

# --- Azure Speech (pronunciation scoring + Korean TTS) ---
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=

# --- NVIDIA API (chat, vision) ---
NVIDIA_API_KEY=

# --- Paddle Billing (optional — billing routes return 503 when unset) ---
# PADDLE_ENV is "sandbox" or "production"; the client token is public.
PADDLE_ENV=sandbox
PADDLE_CLIENT_TOKEN=
PADDLE_WEBHOOK_SECRET=
PADDLE_PRICE_PREMIUM=
PADDLE_PRICE_FOUNDER=

# --- Frontend build (Vite inlines these at build time) ---
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_URL=http://localhost:8000
